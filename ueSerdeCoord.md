# Сериализация координат в БД

> Unreal Engine. как можно сериализовать FVector и FRotator для записи в реляционную БД? компоненты векторов сохранять в отдельные столбцы. как затем десериализовать сохраненные данные? можно ли делать это в Blueprints?

В Unreal Engine сериализовать `FVector` и `FRotator` для реляционной базы данных можно несколькими способами, включая работу в Blueprints. Вот основные подходы:

### ⚙️ Способы сериализации и десериализации

1.  **Разложение на отдельные поля (рекомендуется для БД)**
    Самый простой и наглядный способ — сохранять каждую компоненту вектора и ротатора в отдельный столбец базы данных. Это удобно для запросов и понятно при просмотре данных.

    *   **Для `FVector`:**
        *   **Сериализация:** Разложите вектор на три поля: `X`, `Y`, `Z` (типа `float`).
        *   **Десериализация:** Создайте новый `FVector`, используя три значения из БД: `FVector(X, Y, Z)`.

    *   **Для `FRotator`:**
        *   **Сериализация:** Разложите ротатор на три поля: `Pitch`, `Yaw`, `Roll` (типа `float`).
        *   **Десериализация:** Создайте новый `FRotator`, используя три значения из БД: `FRotator(Pitch, Yaw, Roll)`.

2.  **Сериализация в формат JSON**
    Можно сериализовать структуры в JSON-строку, которую затем сохранять в БД. В Unreal Engine для этого используется API `FJsonObject` и `FJsonSerializer`.

    *   **Сериализация:** Создайте `FJsonObject`, задайте его поля (например, `X`, `Y`, `Z` для вектора), а затем с помощью `TJsonWriterFactory` преобразуйте объект в строку `FString`.
    *   **Десериализация:** Используйте `TJsonReaderFactory` для создания `TSharedPtr<FJsonObject>` из строки, а затем извлеките значения полей.

3.  **Использование встроенной системы сериализации Unreal Engine**
    Unreal Engine использует класс `FArchive` для сериализации объектов. Этот метод чаще применяется для сохранения данных в бинарном виде, но его можно адаптировать. Для передачи данных по сети в структурах может быть реализован метод `NetSerialize`.

### 🔮 Работа в Blueprints

**Да, основные операции можно выполнять в Blueprints.**

*   **Разложение на компоненты:** В Blueprints есть ноды **`Break Vector`** и **`Break Rotator`**, которые разлагают структуры на отдельные компоненты `X, Y, Z` и `Pitch, Yaw, Roll` соответственно.
*   **Сборка из компонентов:** Обратное действие выполняют ноды **`Make Vector`** и **`Make Rotator`**, которые создают структуры из отдельных компонентов.
*   **Работа с JSON:** Для сериализации в JSON можно использовать ноды из категории **`JSON`**, например, **`Construct JSON Object`**, **`Serialize JSON To String`** и обратные им **`Get Number Field`**, **`Deserialize JSON String`**.

### 💡 Примерный алгоритм действий

1.  **Подготовка структуры БД**: Создайте таблицы с отдельными колонками для компонент (например, `pos_x`, `pos_y`, `pos_z`, `rot_pitch`, `rot_yaw`, `rot_roll`).
2.  **Преобразование в движке**:
    *   **Сериализация**: Используйте `Break Vector` и `Break Rotator` в Blueprints или напрямую обращайтесь к полям `X, Y, Z` и `Pitch, Yaw, Roll` в C++, чтобы получить значения для записи в БД.
    *   **Десериализация**: Прочитав значения из БД, используйте `Make Vector` и `Make Rotator` в Blueprints или конструкторы `FVector`/`FRotator` в C++ для восстановления структур.
3.  **Работа с БД**: Для непосредственного взаимодействия с реляционной БД (выполнение SQL-запросов) вам, скорее всего, потребуется плагин (например, `Database Support Plugin`) или написание собственного кода на C++ с использованием соответствующих библиотек.

### 🛠️ Рекомендации

Для реляционных баз данных **наиболее практичным является метод разложения на отдельные столбцы**. Этот подход прозрачен, позволяет легко индексировать данные и выполнять запросы к конкретным компонентам. Сериализация в JSON предоставляет больше гибкости, но усложняет анализ данных средствами БД.
